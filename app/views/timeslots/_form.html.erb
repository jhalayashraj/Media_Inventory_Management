<%= form_with(model: timeslot, local: true) do |form| %>
  <% if timeslot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timeslot.errors.count, "error") %> prohibited this timeslot from being saved:</h2>

      <ul>
      <% timeslot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :timeslot_user_id %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration, id: :timeslot_duration %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :timeslot_price %>
  </div>

  <div class="field">
    <%= form.label :frequency %>
    <%= form.text_field :frequency, id: :timeslot_frequency %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :timeslot_status %>
  </div>

  <div class="field">
    <%= form.label :on_resale %>
    <%= form.number_field :on_resale, id: :timeslot_on_resale %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
