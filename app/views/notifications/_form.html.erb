<%= form_with(model: notification, local: true) do |form| %>
  <% if notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification.errors.count, "error") %> prohibited this notification from being saved:</h2>

      <ul>
      <% notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :notification_user_id %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :notification_message %>
  </div>

  <div class="field">
    <%= form.label :purchased_slot_id %>
    <%= form.text_field :purchased_slot_id, id: :notification_purchased_slot_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
